<!-- Themes/CheckBoxStyle.xaml -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="BzCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- 自定义可缩放勾选框 -->
                        <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                            <Border x:Name="Box"
                                    BorderBrush="LightGray"
                                    BorderThickness="2"
                                    CornerRadius="6"
                                    Background="Transparent"/>

                            <Viewbox Margin="8">
                                <Path x:Name="CheckMark"
                                      Data="M 0 10 L 6 16 L 18 2"
                                      Stroke="White"
                                      StrokeThickness="3"
                                      Visibility="Collapsed"/>
                            </Viewbox>
                        </Grid>

                        <ContentPresenter VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                    </StackPanel>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="#378CF3"/>
                            <Setter TargetName="Box" Property="BorderBrush" Value="#378CF3"/>
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="ScCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- 自定义可缩放勾选框 -->
                        <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                            <Border x:Name="Box"
                                    BorderBrush="LightGray"
                                    BorderThickness="2"
                                    CornerRadius="12"
                                    Background="Transparent"/>

                            <Viewbox Margin="8">
                                <Border x:Name="CheckMark" Width="15" Height="15"
                                        Visibility="Collapsed"
                                        CornerRadius="7.5"
                                        Background="#378CF3"/>
                            </Viewbox>
                        </Grid>


                        <ContentPresenter VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                    </StackPanel>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
