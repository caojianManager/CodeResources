<UserControl x:Class="EEGTool.Views.Common.BrainAreaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EEGTool.Views.Common"
             xmlns:rs="clr-namespace:Resources"
             mc:Ignorable="d"
             d:DesignHeight="599" d:DesignWidth="536" 
             Background="Transparent">
    <UserControl.Resources>
        <Style x:Key="CircleButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="39"/>
            <Setter Property="Height" Value="39"/>
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <!--<Setter Property="Foreground" Value="White"/>-->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="24"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Image Source="{x:Static rs:ResourcesImagePath.BRAIN_AREA}"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"/>
        <Canvas x:Name="EegCanvas" SizeChanged="EEGCanvas_SizeChanged"/>

    </Grid>
</UserControl>
